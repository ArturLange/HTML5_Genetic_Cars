import { expect } from 'chai';
import 'mocha';
import { mapToShuffle } from '../../src/machine-learning/random';

//describe('mapToShuffle function', () => {
//    it('should return predefined values', () => {
//        const prop = {};
//        const normals = {};
//        expect(mapToShuffle(prop, normals)).to.deep.equal(true);
//    });
//});
